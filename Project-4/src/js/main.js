// Empty for now - will add functionality when needed
// (accordion, mobile menu toggle, etc.)

document.addEventListener('DOMContentLoaded', function() {
    // Ready for future interactions
});

// Empty for now - Hero section doesn't need JS interactions
// (except the button which will link to a form/modal later)

document.addEventListener('DOMContentLoaded', function() {
    // Ready for future interactions
});

